{
  "name": "altex-api",
  "version": "1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.19.2",
    "openai": "^4.38.2"
  },
  "engines": {
    "node": "16"  // specify the Node.js version compatible with your project (adjust if needed)
  }
}
